# Server
PORT=5000
NODE_ENV=development

# MongoDB (optional — for full MERN persistence)
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/mernapp

# AWS (used by buildspec.yml — set these in CodeBuild env vars, not here)
# AWS_DEFAULT_REGION=us-east-1
# AWS_ACCOUNT_ID=123456789012
# IMAGE_REPO_NAME=mern-app-repo
